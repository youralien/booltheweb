extends layout

block content
  script(src='/javascripts/home.js')
  //- naviation bar
  nav.navbar.navbar-default(role='navigation')
    div.navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/home') Bool
    div.navbar-collapse.collapse
      ul.nav.navbar-nav.navbar-left
        li
          a(href='/ask') New Question
        li
          a(href='/home') Let's Bool! 
      ul.nav.navbar-nav.navbar-right
        li
          a(href='#about') #{user.firstName}
        li
          a(href='/signout') Logout 

    //- div#question-container
  ion-pane(ng-controller='CardsCtrl')
    td-cards
      td-card(ng-repeat='card in cards',on-destroy='cardDestroyed($index)',on-swipe-left='cardSwipedLeft($index)',on-swipe-right='cardSwipedRight($index)',on-partial-swipe='cardPartialSwipe(amt)',ng-controller='CardCtrl').card-{{card.index}}
        .list-group-item
          .row-content
            h4.list-group-item-heading {{card.question}}
            p.list-group-item-text {{card.optionA}} or {{card.optionB}}
          .yes-text(ng-style='leftTextOpacity') LIKE
          .no-text(ng-style='rightTextOpacity') NOPE